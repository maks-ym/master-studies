#include <cstddef>
#include <cstdlib>
#include <string>
#include <iostream>

#include "TreeMap.h"

//namespace
//{
//
//template <typename K, typename V>
//using Map = aisdi::Map<K, V>;
//
//void perfomTest()
//{
//  Map<int, std::string> map;
//  map[1] = "TODO";
//}
//
//} // namespace

int main()
//int main(int argc, char** argv)
{
//  const std::size_t repeatCount = argc > 1 ? std::atoll(argv[1]) : 10000;
//  for (std::size_t i = 0; i < repeatCount; ++i)
//    perfomTest();
//const Map<int, std::string> map = { { 42, "Alice" }, { 27, "Bob" } };
//
//
////    for(aisdi::TreeMap<int, std::string>::iterator it = other.begin(); it != other.end(); ++it ) {
////        std::cout <<  (*it).second << std::endl;
////        std::cout << (*it).first << std::endl;
////    }
//
//    std::cout << (*map.find(key)) << std::endl;
  return 0;
}
